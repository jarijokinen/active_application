<!DOCTYPE html>
<html lang="<%= locale %>">
  <head>
    <title><%= layout_title %></title>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta name="viewport" content="width=device-width" />
    <%= stylesheet_link_tag "active_application/backend", media: "all" %>
    <%= javascript_include_tag "active_application/backend" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="navbar navbar-static-top">
      <nav class="navbar-inner">
        <div class="container">
          <%= link_to(application_name, backend_root_path, class: "brand") %>
          <%= render "layouts/active_application/backend/menu" %>
          <ul class="nav pull-right">
            <% if user_signed_in? %>
              <li><%= link_to(t("devise.links.my_account"), edit_user_registration_path) %></li>
              <li><%= link_to(t("devise.links.sign_out"), destroy_user_session_path, method: :delete) %></li>
            <% else %>
              <li><%= link_to(t("devise.links.sign_in"), new_user_session_path) %></li>
              <li><%= link_to(t("devise.links.sign_up"), new_user_registration_path) %></li>
            <% end %>
          </ul>
        </div>
      </nav>
    </header>
    <section class="container" id="main">
      <%= layout_flash %>
      <%= layout_page_header %>
      <section id="content">
        <%= yield %>
      </section>
      <footer id="footer" class="footer">
        &copy; <%= Time.now.year %> <%= application_name %>
      </footer>
    </section>
  </body>
</html>
